# local session/tool config
.claude/
.DS_Store

# venvs (recreate from requirements-*.txt)
.venv/
.venv-a1/
.venv-midi/
.venv-app/

# runtime artifacts
work/
out/
__pycache__/
*.pyc
*.jsonl
*.log
block_library.json
templates.json
device_scan/
presetExports/

# large binary DI (download separately; see a2a1/README.md)
refs/*.wav

# app build output
app/node_modules/
app/.pytest_cache/
.pytest_cache/

# RE artifacts (large / regenerable)
re/*.dylib
re/ghidra_*.txt
re/*.rep
re/*.gpr

# vendor firmware / manuals (re-download from valeton.net; see re/HTFW_FORMAT.md)
GP-5/
GP-150 Presets/
*.bin

# local NAM A2 capture test data (not part of the repo)
NAM_Files/

# static site build output
dist/
/dist
