[pytest]
markers =
    slow: real end-to-end test that drives the app through a headless browser
          and performs a real (fast-preset) NAM conversion; excluded from the
          default fast suite via `-m "not slow"`.
